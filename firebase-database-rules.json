{
  "rules": {
    "leaderboard": {
      ".read": true,
      "$entryId": {
        ".write": "auth != null && !data.exists()",
        ".validate": "newData.hasChildren(['name','score','totalTimeMs','createdAt']) && newData.child('name').isString() && newData.child('name').val().length > 0 && newData.child('name').val().length <= 40 && newData.child('score').isNumber() && newData.child('score').val() >= 0 && newData.child('totalTimeMs').isNumber() && newData.child('totalTimeMs').val() >= 0 && newData.child('createdAt').isNumber()"
      }
    }
  }
}